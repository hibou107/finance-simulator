package com.hibou107.finance

object TaxCalculator {
  def compute(input: Vector[MultiCashFlow]): Vector[MultiCashFlow] = {
//    input.grouped(12).map { flows =>
//      val x = flows.flatMap(_.values).map {
//        case x: RentalIncome | SalaryIncome |
//      }
//
//
//    }
    ???
  }

}
